import cv2
import time
from pybaby.models.base_cam import BaseCamera

capture = cv2.VideoCapture("tcp://0.0.0.0:8080")


class Camera(BaseCamera):
    @staticmethod
    def frames():
        while True:
            ret, frame = capture.read()
            yield frame
